apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: memory-api
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: memory-api
                port:
                  number: 80
